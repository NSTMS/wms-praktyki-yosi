export type product = {
  id: number,
  productName: string,
  EAN: string,
  price: number,
  quantity: number
}

export type productToAdd = {
  productName: string,
  EAN: string,
  price: number,
  quantity: number
}
